# Ambient Healthcare Agents Developer Example

Make sure that the keys are added in:

ace-controller-voice-interface/ace_controller.env
ace-controller-voice-interface/config.ts
agent/vars.env


the steps in the jupiter notebook also change the content of the above files and set the RTC configuration


manually changed is `/agent/vars.env` with `NEMO_GUARDRAILS_CONFIG_PATH=`